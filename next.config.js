/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "randomuser.me", "placehold.co"],
  },
};

module.exports = nextConfig;
